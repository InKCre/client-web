[project]
name = "inkcre-client-web-tooling"
version = "0.0.0"
description = "Repository-level development tooling for the InKCre Web monorepo"
requires-python = ">=3.13,<3.14"
# Gate: generate and commit pdm.lock only after this immutable Toolkit wheel is published.
dependencies = [
  "inkcre-extension-toolkit[cli] @ https://github.com/InKCre/ext-reg/releases/download/toolkit-v0.1.0/inkcre_extension_toolkit-0.1.0-py3-none-any.whl",
]

[tool.pdm]
distribution = false
