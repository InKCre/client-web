# InkButton

`InkButton` 是一个轻量的按钮组件，使用 `ink` 命名空间避免与原生 `button` 冲突。

Props

- `variant?: 'primary' | 'default'` - 样式变种，默认是 `default`。

Slots

- 默认插槽用于按钮内容。

Events

- `click` - 当按钮被点击时触发，事件参数为原生 MouseEvent。
