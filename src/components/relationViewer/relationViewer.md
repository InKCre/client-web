# RelationViewer

用于展示一条关系（relation）以及其两端的 block 槽位。

Props:

- `relation: Relation` — 关系对象。
- `to?: boolean` — 是否渲染 to 端。
- `from?: boolean` — 是否渲染 from 端。
- `mode?: 'wrap_block'` — 可选模式。

样式在 `relationViewer.scss`，类型在 `relationViewerTypes.ts`。
